[project]
name = "algoritmit-aine"
version = "0.1.0"
description = ""
authors = [
    {name = "Oskari",email = "oskari.lamsa@helsinki.fi"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pygame (>=2.6.1,<3.0.0)",
    "pillow (>=12.0.0,<13.0.0)",
    "invoke (>=2.2.1,<3.0.0)",
    "pytest (>=9.0.1,<10.0.0)",
    "coverage (>=7.12.0,<8.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
